С++ ООП Домашнее задание №2

1) Дописать класс точка_3д: добавить геттеры и сеттеры
	 getX(), getY() getZ(), setX(), setY() setZ().
2) Написать функцию, принимающую параметр  point_3d по значению
void getPoint3d(point_3d p)
2) Написать функцию, принимающую параметр  point_3d по указателю
void getPoint3d(point_3d* p)
3) Самое главное — в каждую функцию и метод (конструтор в том числе) — добавить печать «где вы (код) сейчас находитесь» - мы делали это в классе — нужно дописать.
Чтобы запуская программу, которая вызывает getPoint3d — было видно какой конструктор и деструктор запускается
4) Написать деструктор и в него добавить печать «я в деструкторе»

Суть задания — посмотреть и понять когда вызывается конструктор и деструктор класса. Для этого в функции main() надо содавать переменные класса point_3d статически, через new, создавать массивы точек и удалять их.
И вызывать функции getPoint3d с разными параметрами.

Деструктор , если забыли, выглядит примерно так:

class point_3d {
    double x,y,z;
public:
    point_3d();
    …
    …
    ~point_3d();
}

point_3d::~point_3d() {
    cout << "I am destructor" << endl;
}